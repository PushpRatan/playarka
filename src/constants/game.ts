/** Duckpin lane: 8 standing pins per frame (matches backend + firmware). */
export const PINS_PER_FRAME = 8;

/** Standard 10-frame game. */
export const FRAMES_PER_GAME = 10;

/** Max possible total for this scoring model (backend `maxScore`). */
export const MAX_SCORE = 280;
