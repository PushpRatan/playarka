// Configuration example
// Copy this to config.js and update with your values

export const config = {
  mqtt: {
    broker: 'mqtt://localhost:1883', // Change to your MQTT broker URL
    username: '', // Optional
    password: '', // Optional
  },
  deviceId: '1',
  port: 3001,
};

